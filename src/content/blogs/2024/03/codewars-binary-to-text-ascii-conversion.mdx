---
title:
pubDate: 2024-03-16
description:
category: coden
tags:
series: code challenges
vgWortCode: 5ef5ec1251c2466c82b5a0fcebca196a

codeChallengeData:
  platform: codewars
  id: 5583d268479559400d000064
  language: TypeScript
---

## Inhalt

1. [Die Fakten](#)
1. [Beschreibung](#)
1. [Lösung](#)
   1. [Pseudo-Code](#)
   1. [Code](#)
1. [Feedback](#)

---

## Die Fakten:

|                |                                                                                                                                            |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| **Plattform:** | <ExternalLink href={baseURLs[codeChallengeData['platform']]}>{domains[codeChallengeData['platform']]}</ExternalLink>                       |
| **Name:**      | <ExternalLink href={`${codeChallenge[codeChallengeData['platform']].challenge.url}/${codeChallengeData.id}`}>{apiData.name}</ExternalLink> |
| **Level**:     | {apiData.level}                                                                                                                            |
| **Sprache**:   | {codeChallengeData.language}                                                                                                               |

## Beschreibung:

<FetchedMarkdown>{apiData.description}</FetchedMarkdown>

Quelle: <ExternalLink href={baseURLs[codeChallengeData['platform']]}>{domains[codeChallengeData['platform']]}</ExternalLink>

## Lösung

### Pseudo-Code

Wie immer gibt's reichlich Varianten, hier ist eine meiner.

Erst die Lösungsschritte in Pseudo-Code. Los geht’s:

<Accordion>
**Lösungsschritte**

<Accordion>
Schritt 1

Wir loopen durch das Input-Binary.

</Accordion>
<Accordion>
Schritt 2

In jeder Iteration schnappen wir uns die nächsten `8` Zeichen.

</Accordion>
<Accordion>
Schritt 3

Diese `8` Bits wandeln wir in ihre dezimale Entsprechung um.

</Accordion>
<Accordion>
Schritt 4

Und speichern das Ergebnis in einer Variablen.

</Accordion>
</Accordion>

### Code

Geil. Übersetzen wir unseren Pseudo-Code in {codeChallengeData.language}:

<Accordion>
**Lösungsschritte**

<Accordion>
Meine erste Zeile:

```ts
export function binaryToString(binary: string): string {
```

</Accordion>
<Accordion>

Als Erstes der Loop:

```ts
  for (let i = 0; i < binary.length; i += 8) {
```

`i` erhöhen wir hier bei jeder Iteration um `8`.

</Accordion>
<Accordion>

Jetzt schnappen wir uns das aktuelle `8`-Bit Stück und speichern es in einer Variablen zwischen:

```ts
const curr8Bits = binary.slice(i, i + 8);
```

</Accordion>
<Accordion>

Dann können wir das aktuelle `8`-Bit Stück in eine Zahl, den ASCII-Code umwandeln:

```ts
const currCharCode = parseInt(curr8Bits, 2);
```

</Accordion>
<Accordion>

Und jetzt den aktuellen ASCII-Code in ein Zeichen/Buchstaben umwandeln und zu einer Ergebnis-Variablen hinzufügen:

```ts
    result += String.fromCharCode(currCharCode);
  }
```

Die Variable muss natürlich noch _oben_ initialisiert werden.

```ts
let result = "";
```

</Accordion>
<Accordion>

Nach dem Loop nur noch das Ergebnis zurückgeben:

```ts
  return result;
}
```

</Accordion>
<Accordion>

Voilá! 💪

[Fragen?](#feedback)

</Accordion>
</Accordion>

<Accordion>
**Komplettlösung**

```ts
export function binaryToString(binary: string): string {
  let result = "";

  for (let i = 0; i < binary.length; i += 8) {
    const curr8Bits = binary.slice(i, i + 8);
    const currCharCode = parseInt(curr8Bits, 2);

    result += String.fromCharCode(currCharCode);
  }

  return result;
}
```

</Accordion>

## Feedback

<Accordion>
**Schreib mir!**

<SocialLinks
  emailSubject={`Frage zu ${apiData.platform} "${apiData.name}" (${codeChallengeData.language})`}
/>

</Accordion>
