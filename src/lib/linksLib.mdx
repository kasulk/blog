{/* pre-created links for copy/paste */}

{/* array methods */}

<ExternalLink href={docs.array.map}>`.map()`</ExternalLink>;
<ExternalLink href={docs.array.reverse}>`.reverse()`</ExternalLink>
<ExternalLink href={docs.array.slice}>`.slice()`</ExternalLink>

{/* statements */}

<ExternalLink href={docs.statements.for}>`For-Loop`</ExternalLink>;
